# A0 Task 2 — 8페이지 4그룹 분류 + 토큰 매핑표 확정

**작성일**: 2026-04-28 17:30
**상위 MBO**: v5 (APPROVED) — A1 Task 1·3에서 사용 예정
**참조**: v5 행 102~123 (4그룹 분류 룰)

---

## 📋 8페이지 4그룹 매핑

| # | 파일 | 그룹 | 적용 룰 단계 | 1차 컬러 | 보조 액센트 | 시각 정체성 |
|:---:|---|:---:|:---:|---|---|---|
| 1 | `index.html` | **Conversion** | 2 (전환 목적) | `--brand` (#f59e0b) | `--brand-700` | 행동 유도 |
| 2 | `pages/ax-project.html` | **Conversion** | 2 (전환 목적, `<form>`) | `--brand` | `--brand-700` | 행동 유도 |
| 3 | `about-ax.html` | **Content** | 3 (콘텐츠 유형) | `--brand` | `--brand-100~300` | 차분·신뢰 |
| 4 | `methodology.html` | **Content** | 3 (콘텐츠 유형) | `--brand` | `--brand-100~300` | 차분·신뢰 |
| 5 | `community.html` | **Community** | 3 (사용자 참여) | `--brand` + `--accent-warm` | `--accent-warm` | 활기·소속감 |
| 6 | `pool.html` | **Community** | 3 (사용자 참여) | `--brand` + `--accent-warm` | `--accent-warm` | 활기·소속감 |
| 7 | `terms.html` | **Legal** | 1 (URL 패턴) | `--accent-neutral` (slate-500) | `--accent-neutral-strong` (slate-700) | 규제·신뢰 |
| 8 | `privacy.html` | **Legal** | 1 (URL 패턴) | `--accent-neutral` (slate-500) | `--accent-neutral-strong` (slate-700) | 규제·신뢰 |

> **별도 처리**: `contact.html`은 단순 폼 → Conversion 그룹 편입 검토 (PR 시점 결정)

---

## 🎨 토큰 정의 마스터 (`css/common.css` 권장 위치)

```css
:root {
  /* ───────── 축 1: 브랜드 컬러 ───────── */
  --brand: #f59e0b;          /* amber-500 */
  --brand-100: #fef3c7;
  --brand-300: #fcd34d;
  --brand-500: #f59e0b;
  --brand-700: #b45309;
  --brand-900: #78350f;

  /* ───────── 축 1: 보조 액센트 ───────── */
  --accent-warm: #fb923c;             /* orange-400, Community */
  --accent-neutral: #64748b;          /* slate-500, Legal 1차 */
  --accent-neutral-strong: #334155;   /* slate-700, Legal 강조 */

  /* ───────── 축 1: 텍스트·배경 ───────── */
  --text-primary: #0f172a;            /* slate-900 */
  --text-muted: #64748b;
  --bg-base: #ffffff;
  --bg-elevated: #f8fafc;
  --border-subtle: #e2e8f0;

  /* ───────── 축 2: 타이포 사이즈 ───────── */
  --fs-hero: 56px;
  --fs-page-title: 40px;
  --fs-section: 32px;
  --fs-card: 20px;
  --fs-lead: 18px;
  --fs-body: 16px;
  --fs-small: 14px;

  /* ───────── 축 2: 타이포 weight ───────── */
  --fw-regular: 400;
  --fw-medium: 500;
  --fw-semibold: 600;
  --fw-bold: 700;
  --fw-black: 900;

  /* ───────── 축 3: 간격 (8px 기반) ───────── */
  --sp-1: 4px;
  --sp-2: 8px;
  --sp-3: 12px;
  --sp-4: 16px;
  --sp-6: 24px;
  --sp-8: 32px;
  --sp-12: 48px;
  --sp-16: 64px;
  --sp-24: 96px;

  /* ───────── 축 4: 컴포넌트 — radius ───────── */
  --r-sm: 4px;
  --r-md: 8px;
  --r-lg: 12px;
  --r-xl: 20px;
  --r-full: 9999px;

  /* ───────── 축 4: 컴포넌트 — shadow ───────── */
  --sh-1: 0 1px 2px rgba(15, 23, 42, 0.04);
  --sh-2: 0 4px 12px rgba(15, 23, 42, 0.08);
  --sh-3: 0 12px 32px rgba(15, 23, 42, 0.12);

  /* ───────── 축 4: 컴포넌트 — icon ───────── */
  --icon-sm: 16px;
  --icon-md: 20px;
  --icon-lg: 24px;
  --icon-xl: 32px;
}
```

> **⚠️ 단, A1 진입 후 적용**: 본 토큰 세트는 A0 게이트 통과 후 A1 Task 1에서 `common.css`에 실제 작성한다.

---

## 📐 그룹별 사용 패턴

### Conversion 그룹 (index, ax-project)

```css
/* Hero CTA — 강한 색 대비 */
.btn-primary {
  background: var(--brand);
  color: var(--bg-base);
  font-weight: var(--fw-bold);
}
.btn-primary:hover { background: var(--brand-700); }

/* 섹션 헤더 강조 */
.sec-title { color: var(--brand-700); }
```

### Content 그룹 (about-ax, methodology)

```css
/* 차분한 액센트 */
.eyebrow { color: var(--brand-300); }
.sec-title { color: var(--text-primary); border-bottom: 2px solid var(--brand-100); }

/* methodology.html 기존 teal → amber 통일 (A1 Task 4) */
```

### Community 그룹 (community, pool)

```css
/* 따뜻한 보조 액센트 */
.tag-active { background: var(--accent-warm); color: var(--bg-base); }
.eyebrow { color: var(--accent-warm); }
```

### Legal 그룹 (terms, privacy)

```css
/* 무채색 1차 + 강조용 slate-700 */
.sec-title { color: var(--accent-neutral-strong); }   /* slate-700 */
.lead { color: var(--accent-neutral); }                /* slate-500 */
.btn-secondary { border-color: var(--accent-neutral); }
```

---

## 🔍 grep 검수 룰 (PR 머지 전 자동 검사)

```bash
# 1. 컬러 hard-coded 검출 (:root 외부)
grep -rn "#[0-9a-fA-F]\{3,6\}" --include="*.css" --include="*.html" \
  | grep -v ":root" | grep -v "common.css"
# → 0건이어야 통과

# 2. font-size 직접 지정 검출
grep -rn "font-size:\s*[0-9]" --include="*.css" --include="*.html" \
  | grep -v "var(--fs-"
# → 0건이어야 통과

# 3. font-weight 숫자 직접 지정
grep -rn "font-weight:\s*[0-9]" --include="*.css" \
  | grep -v "var(--fw-"
# → 0건이어야 통과

# 4. 'teal' 사용 검출 (Content 그룹 통일 후)
grep -rn "teal\|#14b8a6\|#5eead4" --include="*.css" --include="*.html"
# → 0건이어야 통과 (A1 Task 4 완료 후)

# 5. warm-orange 변수명 잔존 검사 (DA w2 반영)
grep -rn "warm-orange" --include="*.css" --include="*.html"
# → 0건이어야 통과 (--accent-warm으로 통일)
```

---

## ✅ PR 머지 체크리스트 (신규 페이지 추가 시)

- [ ] 그룹 배정 룰 단계 명시 (URL/전환목적/콘텐츠유형 중 1개)
- [ ] 그룹별 사용 패턴 따름
- [ ] 4축 정합성 — 새 토큰 추가 시 카테고리 명시
- [ ] grep 5개 룰 모두 0건 통과
- [ ] Lighthouse 모바일 점수 -10 미만 회귀 없음

---

## 📦 산출물

본 매핑표는 다음 시점에 활용:

| 시점 | 활용 |
|---|---|
| **A0 측정 중** | Vercel Analytics 그룹 태그 자동 분류에 사용 (Task 50 완료) |
| **A1 Task 1** | `common.css` :root 작성 시 토큰 정의 그대로 복붙 |
| **A1 Task 3** | 4그룹 분류 + 보조 액센트 적용 시 본 표 참조 |
| **A1 Task 6** | grep 검수 5개 룰 자동 실행 |

다음 단계: Task 53 (벤치마크 옵션 결정)
